language: python
matrix:
  include:
    - name: "Unit Tests"
      python: "3.6"
install:
  - pip install -r requirements-dev.txt
  - pip install .
  - touch conf.py
script: pytest -m "not integration"
